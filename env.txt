# .db/.env
    SUPABASE_URL=supabase-url
    SUPABASE_ANON_KEY=anon-key
    DATABASE_URL=db-url
    DIRECT_URL=direct-url

# .local/.env
    APP_PORT=8000
    APP_HOST=http://localhost:8000
    FRONTEND_URL=http://localhost:3000/
    JWT_ACCESS_TOKEN_SECRET=anyKey
    JWT_ACCESS_TOKEN_EXPIRATION_TIME=900000
    JWT_REFRESH_TOKEN_SECRET=anyRefreshKey
    JWT_REFRESH_TOKEN_EXPIRATION_TIME=1800000

    # For multiple origins, separate with commas
    CORS_ORIGINS=http://localhost:3000,http://localhost:4000
    PASSPORT_SECRET=your-secret-key

# .prod/.env
    APP_PORT=8000 # Need to update
    APP_HOST=app-host
    FRONTEND_URL=production-url
    JWT_ACCESS_TOKEN_SECRET=anyKey
    JWT_ACCESS_TOKEN_EXPIRATION_TIME=900000
    JWT_REFRESH_TOKEN_SECRET=anyKey
    JWT_REFRESH_TOKEN_EXPIRATION_TIME=1800000
    # For multiple origins, separate with commas
    CORS_ORIGINS=cors-origins
    PASSPORT_SECRET=secret-key

# .utils/.env
    # DeepSeek Config
    DEEPSEEK_API_URL=https://api.deepseek.com
    DEEPSEEK_API_KEY=apiKey

    # OpenAI Config
    OPENAI_API_KEY=openAIKey

    # Google Oauth config
    GOOGLE_CLIENT_ID=clientID
    GOOGLE_CLIENT_SECRET=clientSecret
    GOOGLE_AUTH_REDIRECT_URL=/user/google/callback
    GOOGLE_OAUTH_URL=https://accounts.google.com/o/oauth2/v2/auth
    GOOGLE_ACCESS_TOKEN_URL=https://oauth2.googleapis.com/token
    GOOGLE_TOKEN_INFO_URL=https://oauth2.googleapis.com/tokeninfo

    # AWS credentials
    AWS_ACCESS_KEY_ID=accessKey
    AWS_SECRET_ACCESS_KEY=secretAccessKey
    AWS_S3_BUCKET_NAME=bucket-name
    AWS_S3_REGION=aws-region

    #Langsmith
    LANGSMITH_TRACING=true
    LANGSMITH_ENDPOINT=https://api.smith.langchain.com
    LANGSMITH_API_KEY=apiKey
    LANGSMITH_PROJECT=projectKey

